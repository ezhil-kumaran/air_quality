name: air_quality_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  geolocator: ^14.0.1
  http: ^1.4.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logoad.png"
  adaptive_icon_foreground: "assets/logoad.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
